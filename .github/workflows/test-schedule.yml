name: Scheduler Test

on:
  schedule:
    - cron: "*/5 * * * *"    # every 5 minutes
  # no workflow_dispatch on purpose

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggered automatically at $(date -u)"
